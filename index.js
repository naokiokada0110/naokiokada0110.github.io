$(function(){
  ScrollReveal().reveal('h1',{
    duration:1500,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('.twitter',{
    duration:2500,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('.instagram',{
    duration:3000,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('.img_face',{
    duration:2000,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('dt',{
    duration:2500,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('dd',{
    duration:2500,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('.textbox',{
    duration:2500,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('.photo',{
    duration:2000,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('.text',{
    duration:2500,
    viewFactor: 0.2, 
  });

  ScrollReveal().reveal('button',{
    duration:2500,
    viewFactor: 0.2, 
  });
  
  ScrollReveal().reveal('.contact_text',{
    duration:2000,
    viewFactor: 0.2, 
  });
  
  ScrollReveal().reveal('iframe',{
    duration:2500,
    viewFactor: 0.2, 
  });
  
});
